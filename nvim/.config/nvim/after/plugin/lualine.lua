local lualine = require('lualine')

lualine.setup({
    options = {
        icons_enabled = true,
        globalstatus = true,
        theme = 'auto',
        component_separators = '|',
        section_separators = '',
    }
})
