# dotfiles

Configuration files managed with [GNU Stow](https://www.gnu.org/software/stow/).

## quick start

Run `install` script to install the dotfiles. It assumes the dotfiles repo is cloned to `$HOME/.dotfiles`.
